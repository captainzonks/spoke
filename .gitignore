# ==============================================================================
# SPOKE - GITIGNORE
# ==============================================================================
# Description: Git ignore rules for Spoke hub-and-spoke server platform
# Author: Matt Barham
# Created: 2026-02-12
# ==============================================================================

# Site-specific configuration (contains personal domain, IPs, paths)
shared/env/base.env
shared/env/hub.env
modules.yml

# Secrets (passwords, API keys, certificates)
/secrets/
/secrets

# Cloned module repositories (top-level only)
/modules/

# Runtime application data
/appdata/

# Generated environment files
hub/.env

# Docker build cache
build/

# Security scanning cache
.cache_ggshield

# Local MCP server configuration (instance-specific paths/credentials)
.mcp.json

# Editor and OS files
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# Local / private script inputs (server-specific: known log patterns, etc.).
# Not for public release. Drop any such file under scripts/local/.
scripts/local/

# Local / private docs (server-specific: live IP references, runbooks, secret
# layouts). Not for public release. Drop any such doc under docs/local/.
docs/local/

# Log files
*.log
logs/

# Backup files
backups/
*.bak
